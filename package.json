{
  "name": "ea-mongoose",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "example:simple": "npm run build && node dist/examples/01-simple.js",
    "example:promises": "npm run build && node dist/examples/02-promises.js",
    "example:async": "npm run build && node dist/examples/03-async-await.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongoose": "^6.13.11"
  }
}
